与上个版本的不同：
1.接口函数变了，从前是每次提交packetInfo的时候由这个函数自己创建结果集，完了之后调用者要关闭结果集。
现在是这个函数不创建结果集，而由调用者事先创建，这样调用者在初始化中创建一遍就行了，在调用这个函数后也不需要关闭。

2.解决了一个bug，之前是每当有非syn包，没创建的创建，这样会导致tcp跟踪没法释放。
因为释放包是,fin，抓到后释放,再fin ack，创建,再fin，再释放，fin ack再创建，这样的漏洞。
现在是bodyLen==0时不创建了，就避免了像这样，fin包来，抓包后释放，fin ack的不会创建了。

